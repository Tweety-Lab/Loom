import Standard::Windows;

module Standard::Program
{
    /// @brief Program Entry Point
    export i32 Main()
    {
        i32 exitCode = 1;
        
        Process process = Process.Current();
        Process copy = process;

        if (copy.Handle == process.Handle)
            exitCode = 2;
        
        return exitCode;
    }
}